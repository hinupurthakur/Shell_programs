#To untar a tar file
tar -xvf <file.tar> 
#To search occurence of specific word in  file
grep "word" file.txt
#To find the folders which contain a non-empty specific file.
ls -l 2.*/Tranliterated_words_first_run.dat | awk '{if ($5 != 0) print $9}'
#To check the System Configuration in Linux
sudo lshw -html > system_info.html # to redirect the output in html file named as system_info.html
google-chrome system_info.html # to display it on a Web Browser
